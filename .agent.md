# .agent.md — Archivo de Reglas Global del Agente

> **Propósito de este archivo:** Este es el contrato maestro entre el equipo humano y los agentes
> de IA (o desarrolladores nuevos) que intervienen en el proyecto. Define quién es el proyecto,
> cómo se construye, qué está permitido y qué no. Todo agente DEBE leer este archivo completo
> antes de escribir una sola línea de código o documentación.
>
> **Instrucción de uso:** Reemplaza cada placeholder con formato `[TEXTO_EN_MAYUSCULAS]` por el
> valor real del proyecto al momento de inicializarlo. No borres ninguna sección: si una sección
> no aplica, escribe explícitamente `N/A` con una justificación de una línea.

---

## 1. Identidad del Proyecto

- **Nombre del Proyecto:** [NOMBRE_DEL_PROYECTO]
- **Breve descripción:** [DESCRIPCION_1_A_2_FRASES]
  > *Guía de redacción:* Responde en máximo dos frases: ¿qué hace el sistema y para quién?
  > Ejemplo genérico de estilo (no copiar literal): "Sistema que permite a [ROL_DE_USUARIO]
  > realizar [ACCION_PRINCIPAL] sin necesidad de [PROBLEMA_EVITADO]."
- **Dueño del producto / Responsable:** [RESPONSABLE_O_EQUIPO]
- **Repositorio:** [URL_DEL_REPOSITORIO]

---

## 2. Stack Tecnológico

> Completa esta tabla cuando se haya tomado la decisión de stack (ver `spec/constitution/tech-stack.md`).
> Hasta entonces, todos los artefactos deben mantenerse agnósticos al lenguaje.

| Categoría            | Selección                                    | Versión mínima | Justificación breve        |
|----------------------|----------------------------------------------|----------------|----------------------------|
| Lenguaje             | [LENGUAJE_Y_VERSION]                         | [VERSION]      | [JUSTIFICACION]            |
| Framework principal  | [FRAMEWORK_PRINCIPAL]                        | [VERSION]      | [JUSTIFICACION]            |
| Librerías clave      | [LIBRERIAS_CLAVE_SEPARADAS_POR_COMA]         | [VERSIONES]    | [JUSTIFICACION]            |
| Base de datos        | [BASE_DE_DATOS]                              | [VERSION]      | [JUSTIFICACION]            |
| Testing              | [HERRAMIENTAS_DE_TESTING]                    | [VERSION]      | [JUSTIFICACION]            |
| Infraestructura      | [PLATAFORMA_DE_DESPLIEGUE]                   | [VERSION]      | [JUSTIFICACION]            |

---

## 3. Comandos Principales

> Define aquí los comandos canónicos del ciclo de desarrollo. Cualquier agente debe usar
> EXCLUSIVAMENTE estos comandos; inventar comandos alternativos está prohibido (ver sección 7).

| Acción                 | Comando                          | Cuándo usarlo                                        |
|------------------------|----------------------------------|------------------------------------------------------|
| Levantar entorno dev   | `COMANDO_DEV = [COMANDO_DEV]`    | Durante el desarrollo local interactivo               |
| Ejecutar tests         | `COMANDO_TEST = [COMANDO_TEST]`  | Antes de cada commit y en cada PR                     |
| Compilar / construir   | `COMANDO_BUILD = [COMANDO_BUILD]`| Antes de desplegar o validar empaquetado              |
| Linter / formateo      | `COMANDO_LINT = [COMANDO_LINT]`  | Antes de cada commit (obligatorio, cero advertencias) |
| Instalar dependencias  | `COMANDO_INSTALL = [COMANDO_INSTALL]` | Tras clonar o cambiar el manifiesto de dependencias |

---

## 4. Estructura del Proyecto

> Mapa visual de la organización de carpetas. Respétala: las rutas de importación, configuración
> y despliegue dependen de esta estructura.

```
[PROYECTO]/
├── .agent.md                  # ESTE archivo: reglas globales del agente
├── README.md                  # Porta de entrada para humanos
├── .gitignore                 # Exclusión universal de artefactos
├── spec/                      # Contratos: QUÉ se construye y POR QUÉ
│   ├── constitution/          # Documentos fundacionales (misión, stack, roadmap)
│   │   ├── mission.md
│   │   ├── tech-stack.md
│   │   └── roadmap.md
│   └── features/              # Una carpeta numerada por feature
│       ├── _TEMPLATE_/        # Plantilla base: copiar y renombrar
│       └── [NNN]-[nombre-tarea]/
│           ├── spec.md        # Requisitos y criterios de aceptación
│           ├── plan.md        # Estrategia técnica y decisiones
│           └── tasks.md       # Checklist secuencial de ejecución
├── doc/                       # Memoria del proyecto (registro de tareas ejecutadas)
│   └── [NNN]-nombre-tarea.md  # Un archivo por tarea completada
└── src/                       # Código fuente (CUANDO aún no hay stack, solo .gitkeep)
    └── ...
```

---

## 5. Reglas y Convenciones

### 5.1 Arquitectura
- Patrón arquitectónico adoptado: [PATRON_ARQUITECTONICO] (p. ej. capas, hexagonal, monolito modular).
- Toda lógica de negocio debe residir en [CAPA_O_DIRECTORIO_DE_LOGICA]; la capa de interfaz
  (HTTP/CLI/UI) solo orquesta y traduce, nunca contiene reglas de negocio.
- Las dependencias apuntan hacia el dominio: [DIRECCION_DE_DEPENDENCIAS].
- Ningún módulo puede importar desde una capa superior hacia una inferior sin justificación escrita en `plan.md`.

### 5.2 Commits y ramas
- Formato de commit: [FORMATO_DE_COMMIT] — convención recomendada Conventional Commits:
  `tipo(alcance): resumen en imperativo` (ejemplo genérico: `feat(autenticacion): agrega validación de token`).
- Tipos permitidos: `feat`, `fix`, `docs`, `refactor`, `test`, `chore`, `perf`.
- Un commit = una unidad lógica de cambio. Nada de "wip", "cambios", "update".
- Ramas: `main` protegida; trabajo en `rama_de_trabajo = [NOMBRE_DE_RAMA]` con prefijo del tipo de cambio.

### 5.3 Tipado y calidad
- Nivel de tipado exigido: [NIVEL_DE_TIPADO] (fuerte/estricto/dinámico con anotaciones).
- Cero advertencias del linter aceptadas en el código nuevo.
- Cobertura mínima de tests: [PORCENTAJE_COBERTURA_MINIMA]% en lógica de negocio.
- Nombres: descriptivos, en [IDIOMA_DE_NOMBRES] (inglés técnico recomendado), sin abreviaturas crípticas.

### 5.4 Manejo de errores
- Nunca silenciar un error: todo bloque de captura debe registrar y/o propagar ([ESTRATEGIA_DE_ERRORES]).
- Los errores de usuario se expresan con mensajes accionables; los técnicos se registran con contexto suficiente para depurar.
- Definir tipos/códigos de error canónicos en [ARCHIVO_O_MODULO_DE_ERRORES] y reutilizarlos.

---

## 6. Qué NO Hacer (prohibiciones explícitas)

1. **NO exponer credenciales:** jamás escribir secretos, tokens, claves API ni contraseñas en el
   código, en la documentación o en el historial de Git. Usar variables de entorno vía `.env`
   (excluido de Git; documentar en `.env.example`).
2. **NO duplicar lógica:** si un comportamiento aparece dos veces, extraerlo a una unidad compartida.
3. **NO modificar fuera del scope:** un agente solo toca archivos requeridos por la feature activa.
   Cambios "aprovechados" van en otra tarea documentada.
4. **NO asumir tecnología no declarada:** mientras `tech-stack.md` tenga placeholders, prohibido
   generar código dependiente de un lenguaje o framework específico.
5. **NO borrar ni reescribir la historia de Git** en ramas compartidas (`push --force` prohibido en `main`).
6. **NO agregar dependencias externas** sin registrarlas en `tech-stack.md` con su justificación.
7. **NO entregar código sin tests** para la lógica de negocio nueva.
8. **NO completar tareas sin actualizar la memoria** (ver sección 7).

---

## 7. Registro de Memoria (obligatorio)

> Este proyecto funciona con memoria persistente en `/doc/`. Es la única fuente de verdad sobre
> lo que ya se decidió y ejecutó.

**Regla obligatoria:** tras finalizar CADA tarea, el agente DEBE crear el archivo
`doc/NNN-nombre-tarea.md` (numeración secuencial de tres dígitos, ver `doc/000-initial-setup.md`
como referencia de formato) conteniendo como mínimo:

- Fecha y nombre de la tarea ejecutada.
- Archivos creados y modificados.
- Decisiones tomadas con su justificación.
- Lecciones aprendidas (qué salió mal, qué se corregiría).
- Próximos pasos pendientes.

**Antes de iniciar cualquier tarea**, el agente DEBE leer `doc/` completo (al menos los últimos
registros) para no contradecir decisiones previas ni repetir trabajo.

Un registro incompleto equivale a una tarea no terminada.
